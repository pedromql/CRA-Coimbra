<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="poolgrammers.cra_coimbra.AlterarDisponibilidade"
    android:orientation="vertical"
    >

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/select_competion" />

    <Spinner
        android:id="@+id/alterar_disponibilidade_pre_convocatoria_spinner"
        android:layout_marginTop="5dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        />

    <ListView
        android:id="@+id/alterar_disponibilidade_pre_convocatoria_lista_sessoes"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        tools:listitem="@layout/session_item"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight="1"
        android:gravity="center|bottom"
        android:orientation="vertical" >

        <android.support.v7.widget.AppCompatButton
            android:id="@+id/alterar_disponibilidade_pre_convocatoria_submit_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="24dp"
            android:background="#3d3b3b"
            android:hapticFeedbackEnabled="true"
            android:textColor="@android:color/white"
            android:text="@string/alterar_disponibilidade_pre_convocatoria_button"
            />

    </LinearLayout>

</LinearLayout>
